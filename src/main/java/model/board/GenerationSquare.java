package model.board;

import model.card.AmmoCard;

public class GenerationSquare extends Square{

    private ;


    public GenerationSquare(Color color, String type, int x, int y){
        super(color, type, x, y);
        this.ammoCard = card;
    }
}
